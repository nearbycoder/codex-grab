# Changesets

This repo uses Changesets to manage versioning and npm releases for the public
`@codex-grab/*` packages.

Common commands:

- `npm run changeset` to create a release note entry
- `npm run version-packages` to apply pending version bumps locally
- `npm run release` to publish changed packages
